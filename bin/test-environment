#! /bin/sh -e

set -x

cd $(dirname $0)/../docker

docker-compose -f docker-compose-test.yml down --remove-orphans
docker-compose -f docker-compose-test.yml build
docker-compose -f docker-compose-test.yml run --rm --service-ports test-vim "${@-sh}"
