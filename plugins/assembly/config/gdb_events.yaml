---

events:
    before_startup:
    after_startup:
        - 'set_highlight_for_breakpoint'
        - 'connect_to_remote'
        - 'run_to_entrypoint'
    before_command:
    after_command:
        - 'check_set_remote'
