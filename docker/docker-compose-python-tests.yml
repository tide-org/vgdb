version: "2"

services:

  test-python-vgdb:
    build:
      context: ..
      dockerfile: ./docker/Dockerfile-python-tests
    working_dir: /work
    tmpfs: /tests
    volumes:
      - ..:/work
      - ../tests/docker_volume_files/gdb_debug_log_settings.yaml:/root/.vim/plugin/vgdb/plugins/config/gdb_debug_log_settings.yaml

